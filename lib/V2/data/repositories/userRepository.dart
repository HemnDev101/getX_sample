import 'package:getx_sample/V2/data/models/user.dart';
import 'package:getx_sample/V2/data/providers/userService.dart';

class UserRepository {
  // UserProvider _userProvider = new UserProvider();

  // Future<User> loginRepository(String email, String password) async {
  //   final response = await _userProvider.userLogin(email, password);
  //   print(email);
  //   return User.fromJson(response);
  // }
}
